# Dependencies / build output live under archestraide/ (see its own .gitignore)
node_modules/
.next/
out/
.DS_Store
.env*
!.env.example
.netlify
*.tsbuildinfo

# Local checks and static-build staging
.test-build/
.api-static-backup/
