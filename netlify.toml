# Static hosting: the optional Claude API route is not deployed.
[build]
  base = "archestraide"
  command = "npm run build:static"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"
